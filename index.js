function declOfNum(n, titles) {    return titles[(n % 10 === 1 && n % 100 !== 11) ? 0 : n % 10 >= 2 && n % 10 <= 4 && (n % 100 < 10 || n % 100 >= 20) ? 1 : 2]}const yearsList = ["Год", "Года", "Лет"]const monthsList = ["Месяц", "Месяца", "Месяцев"]const daysList = ["День", "Дня", "Дней"]const hoursList = ["Час", "Часа", "Часов"]const minuteList = ["Минута", "Минуты", "Минут"]const secondsList = ["Секунда", "Секунды", "Секунд"]function updateTime(containerName, date) {    const startDate = new Date(date);    const currentDate = new Date();    const elapsedTime = currentDate - startDate;    const container = document.querySelector(`.${containerName}`)    const years = Math.floor(elapsedTime / (1000 * 60 * 60 * 24 * 365));    const months = Math.floor((elapsedTime % (1000 * 60 * 60 * 24 * 365)) / (1000 * 60 * 60 * 24 * 30));    const days = Math.floor((elapsedTime % (1000 * 60 * 60 * 24 * 30)) / (1000 * 60 * 60 * 24));    const hours = Math.floor((elapsedTime % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));    const minutes = Math.floor((elapsedTime % (1000 * 60 * 60)) / (1000 * 60));    const seconds = Math.floor((elapsedTime % (1000 * 60)) / 1000);    container.querySelector('.years').innerText = `${years} ${declOfNum(years, yearsList)}`;    container.querySelector('.months').innerText = `${months} ${declOfNum(months, monthsList)}`;    container.querySelector('.days').innerText = `${days} ${declOfNum(days, daysList)}`;    container.querySelector('.hours').innerText = `${hours} ${declOfNum(hours, hoursList)}`;    container.querySelector('.minutes').innerText = `${minutes} ${declOfNum(minutes, minuteList)}`;    container.querySelector('.seconds').innerText = `${seconds} ${declOfNum(seconds, secondsList)}`;}updateTime('start', '2019-10-22');setInterval(() => updateTime('start', '2019-10-22'), 1000);updateTime('second', '2020-09-15');setInterval(() => updateTime('second', '2020-09-15'), 1000);